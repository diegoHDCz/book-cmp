package br.com.ajudafio.theme

import androidx.compose.ui.graphics.Color

/**
 * Paleta de cores extraída da logo "Ajuda Fio".
 */
object AppPallet {

    // Azul (faixa "Ajuda")
    val PrimaryColor = Color(0xFF0460C5)
    val PrimaryColorLight = Color(0xFF097EDA)
    val PrimaryColorDark = Color(0xFF0145AE)

    // Verde (texto/folha "Fio")
    val SecondaryColor = Color(0xFF1B8A3C)
    val SecondaryColorLight = Color(0xFF4AAF29)
    val SecondaryColorDark = Color(0xFF1C6C17)

    // Vermelho (coração com cruz de saúde)
    val AccentColor = Color(0xFFE5100F)

    // Neutros
    val BackgroundColor = Color(0xFFEDF1F3)
    val SurfaceColor = Color(0xFFFFFFFF)
    val TextColor = Color(0xFF1A1D1F)
    val TextColorSecondary = Color(0xFF6B7280)
}
